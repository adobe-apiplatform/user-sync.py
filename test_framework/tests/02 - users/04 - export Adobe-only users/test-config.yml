user_sync:
    # relative path in this argument is relative to working directory
    arguments: -c ../config/user-sync-config.yml --users file ../../../config-common/users.csv --adobe-only-user-action write-file out/users.csv
