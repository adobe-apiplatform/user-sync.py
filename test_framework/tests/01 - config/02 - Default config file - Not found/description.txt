Default config file - Not found

Preconditions:
1. user-sync is pointed to user-sync-config-missing.yml, which is missing.

Actions:
1. Run python user-sync.pex or ./user-sync with -c user-sync-config-missing.yml arguments

Result
Error message appears.
