Default configuration test

Preconditions:
1. user-sync-config.yml is in the same directory as the aed_sync file.

Actions:
1. Run python user-sync.pex or ./user-sync

Result
No error message and the sync is run.
