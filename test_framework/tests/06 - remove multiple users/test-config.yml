user_sync:
    # relative path in this argument is relative to working directory
    arguments: -c ../config/user-sync-config.yml --adobe-only-user-list ../config/users.csv --adobe-only-user-action delete

server:
    # cassette_filename: cassette/get.yml